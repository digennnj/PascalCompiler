prOgraM
   vAr
       character a, b, c :=‘a’;
       Real x := 0, y;:=1
       inteGER k, j := 2.5, m;
// also try no variables and a type that doesn’t exist
Begin
    Write( x + y, b );
// also try using a undeclared variable and no arguments
    x := k- y + j;
    m := y + ( j - k );
    a := c;
// also try arithmetic with a character and arithmetic with an undeclared variable
   reAd( a, y, k );
// also try an arithmetic expression and an undeclared variable. Last, try no arguments:=
END.
// also try adding a statement after the end and the removal of the period

