prOgraM
   vAr
       BOOLEAN less := true, more;
       character a, b, c :='a';
       Real x := 0, y:=1;
       inteGER k, j := 2.5, m;
       String hello="hi!", bye = "goodbye", no;
Begin
    Write( x + y, b, not less );
    x := k - y + j;
    m := y + ( j - k );
    a := c;
{ here is a multiline comment on a single line }
   reAd( a, y, k, no );
   bye := hello + bye;
   bye := bye + “ha”;
   bye := "byebye";
   x := m + k * x;
   j := k / j % m;
BEgin
   more := j-m >= x;
end
   if not more THEN
begin
 m := x * (y - j) / k;
   more := less and more or x < y;
end
Else
   y := k * x + 3;
if less = more then
   if x > m then
      j := k % 4;
else
   no := less;
while more and not x * y > j do
   if b = a or less then
       repeat
           write(hello + bye);
           read(more);
       until not (true = false)
repeat
   if x < y then
       while j <= 2 do
       begin
           read( a, j );
       end
until k * j = m
END.

