prOgraM 
  vAr 
      character a, b, c :=a; 
      Real x := 0, y:=1 ;
      inteGER k := 1, j := 2.5, m, z := 0, five :=5;
      boolean yy := true;
      StRing w;
Begin 
   Write( x + y, b );
   x := k- y * j; 
   m := y + ( j / k ); 
   a := c;
  reAd( a, y, k, j ); 
  k := k mod j;
  x := 1 + 1;
  x := z / five;
  yy := false;
END. 