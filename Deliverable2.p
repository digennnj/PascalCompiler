prOgraM
   vAr
       boOlean r:=true;
       character a, b, c :='a';
       Real x := 0, y:=1;
       inteGER k, j := 2.5, m;
Begin
    Write( x + y, b );
    x := k- y + j;
    m := y + ( j - k );
    a := c;
   reAd( a, y, k );
END.

