PROGRAM
var
integer in;
BEGIN
    read(in);
    IF 1 = in THEN
        write("A");
    ELSE IF 2 = in THEN
        write("B");
    ELSE IF 3 = in THEN
        write("C");
    ELSE IF 4 = in THEN
        write("D");
    ELSE
        write("E");
END.
