prOgraM
   vAr
       character a, b, c :='a';
       Real x := 0, y:=1;
       inteGER k, j := 2.5, m;
       boOlean r:=true and false or true,s:=false or trUe,t,u:=r=false,z:=y<=j and false,aa:=2=1+1 and u;
    string str:="hello world!", dunder:="__";
Begin
    Write( x + y, b , 3<=3.0, 2.9<=3.0, y<=j and false, not not true and not u, not 4<>4.0);
    write(4+9*7%6/0.9-1);
    write("hello!!"+" world!!" = "hi {world}...");
    x := k- y + j;
    m := y + ( j - k );
    a := c;
   reAd( str, a, y, k );
// write("good bye!");
{ write("parting is such sweet sorrow");
  write("that I should say goodbye till it were morrow!");}write("oh hey I'm back!");
END.

